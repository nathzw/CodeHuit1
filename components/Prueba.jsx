import React from "react";
import styles from "../styles";

const Prueba = () => (
    <section className={`${styles.paddings} py-4 relative`}>
        <div className={`${styles.flexCenter} ${styles.paddings} flex-col`}>
            <h1 className="text-black text-[58px] uppercase">Welcome</h1>
        </div>
    </section>
)

export default Prueba;